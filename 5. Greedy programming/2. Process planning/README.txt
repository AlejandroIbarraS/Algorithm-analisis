This algorithm can determine which items can be store in a bag so that we can fill at maximum capacity and have the biggest value.

Process planning.
This algorithm can determine in which order a proccesor should execute different processes.
